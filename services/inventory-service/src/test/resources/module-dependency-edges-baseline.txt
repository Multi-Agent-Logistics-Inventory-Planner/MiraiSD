# Reviewed, approved set of (source top-level package -> target top-level package) dependency
# edges for com.mirai.inventoryservice production code. See
# ArchitectureTest.moduleDependencyEdgesMatchApprovedBaseline for how this is enforced: a new
# edge here requires deliberate review and an explicit addition to this file, matching
# docs/specs/spring-domain-modular-monolith.md's dependency rules; a *removed* edge (module A no
# longer depends on module B at all) never fails the check.
#
# IMPORTANT - pruning is a review responsibility, not automatic: when a reviewed change removes
# an edge (module A no longer depends on module B anywhere), delete that line from this file in
# the SAME review. The check does not do this for you - it only rejects new edges, so a stale
# line left behind after a real removal would let that dependency return later, unreviewed and
# undetected, if some other change reintroduces it. Leaving stale lines in place quietly weakens
# this file from "what's currently approved" to "everything ever approved," which is not the same
# guarantee.
#
# Generated 2026-09-08 as (edges on commit 85032cd, immediately pre-Phase-5c-T4) plus exactly one
# reviewed addition: `catalog -> sites`, from catalog.application.MainSiteResolver depending on
# sites.application.SiteDirectory (.specs/phase-5c-site-products AC-5's MAIN-resolution seam,
# T-4). Verified before approval: `sites` has zero outgoing edges into `catalog` or anything that
# reaches it (only `sites -> shared`), so this addition is a DAG leaf edge, not a new cycle -
# confirmed both by grepping every import in sites/** (only sites.* and shared.* appear) and by
# this file having no `sites -> catalog` (or `sites -> <anything but shared>`) entry.
#
# Second reviewed addition: `catalog -> shared`, from catalog.api.SiteProductController reading
# shared.web.AuthorizedSiteContextHolder (.specs/phase-5d-catalog-v1-and-web T-2, the same
# site-scoped-controller pattern sites.api.SiteStorageLocationController already uses via
# `sites -> shared`). Safe for the same reason as the `catalog -> sites` addition above: `shared`
# has zero outgoing edges in this file (it is a pure leaf, same as it is for every other module
# already depending on it - auth, identity, services, sites), so this cannot create a new cycle.
#
# Third reviewed addition: `inventory -> identity`, from inventory.application.LastActorActivityAdapter
# implementing identity.application.LastActorActivityPort (.specs/phase-6-inventory/log.md T-2,
# R-3). Resolves a real identity/inventory cycle risk (identity.application.UserService needed
# stock-movement data for its last-activity display; inventory needs a managed identity.domain.User
# for AuditLog.user, tracked separately as R-4) by inverting the dependency: the port lives in
# identity (the consumer), the adapter lives in inventory (the provider), so the edge runs one way
# only. Paired removal in the SAME review: `identity -> models` and `identity -> repositories` are
# deleted below - UserService was their only source in identity/** (verified by grep), and it no
# longer imports StockMovement or StockMovementRepository now that it delegates to the port.
# `identity -> inventory` does NOT appear anywhere in this file, confirmed by grep - approving only
# the `inventory -> identity` direction is what keeps this a DAG edge rather than a cycle the
# moduleDependencyEdgesMatchApprovedBaseline check cannot itself detect once both directions are
# individually approved (see LastActorActivityPort's Javadoc for the same point).
#
# Fourth reviewed addition (2026-09-09, .specs/phase-6-inventory/log.md T-3): the mechanical move
# of LocationInventory, StockMovement, their repositories/specifications/projection, and the three
# inventory exceptions out of the legacy `models`/`repositories`/`exceptions` packages into
# `inventory.domain`/`inventory.infrastructure`. This is a pure package relocation - every one of
# the edges below already existed as `<source> -> models`, `<source> -> repositories`, or
# `<source> -> exceptions` (already approved) before the move; only the *target* package name
# changed, not the underlying dependency:
#   `analytics -> inventory`   (SalesRollupRecomputeService -> StockMovementRepository)
#   `controllers -> inventory` (legacy inventory controllers -> the moved repositories)
#   `dtos -> inventory`        (AuditLogMapper/StockMovementMapper/AuditLogSpecifications -> StockMovement)
#   `exceptions -> inventory`  (GlobalExceptionHandler -> the three moved exception classes)
#   `models -> inventory`      (AuditLog.movements -> StockMovement; the R-2 transitional-debt edge,
#                                the mirror side of the pre-existing `inventory -> models` edge -
#                                recorded, not newly introduced: this is the SAME bidirectional
#                                AuditLog<->StockMovement JPA association that already existed
#                                entirely inside `models.audit` before the move, so rule 6.1.8
#                                treats this as an existing relationship carried across the
#                                boundary, not a new cross-module JPA relationship)
#   `services -> inventory`    (StockMovementService/LocationInventoryService/ShipmentService/
#                                KujiBoxService/MachineDisplayService -> the moved classes)
# Two edges are genuinely new dependency *directions*, both from code that already lived inside
# the moved files, now recomputed against the new module boundary rather than freshly written:
#   `inventory -> sites`  (LocationInventory's `location`/`site` associations -> sites.domain)
#   `inventory -> dtos`   (InventoryTotalsRepository -> dtos.responses.InventoryTotalDTO;
#                          StockMovementSpecifications -> dtos.requests.AuditLogFilterDTO)
#   `inventory -> utils`  (InventoryTotalsRepository -> utils.TimestampUtils)
# `inventory -> dtos` paired with the `dtos -> inventory` edge above is the second genuinely
# bidirectional pair in this file (alongside `models <-> inventory`) - same pre-existing-tangle
# character, not new coupling introduced by this move.
# Pruned in the SAME review: `inventory -> repositories` - grep confirmed `inventory/**` no longer
# imports anything from the legacy `repositories` package once its own repositories moved out of it.
#
# Fifth reviewed addition (2026-09-09, .specs/phase-6-inventory/log.md T-4): moved
# StockMovementService, LocationInventoryService and InventoryAggregateService out of the legacy
# `services` package into `inventory.application`. Mechanical, same underlying dependencies as
# before under a renamed source package - no behavior change. Re-adds `inventory -> repositories`
# (pruned two entries above): StockMovementService still reaches `AuditLogRepository` via the
# `repositories.*` wildcard import (AuditLogRepository itself has not moved - that is `audit`
# module territory, Stage G/Phase 7, out of this checkpoint's scope). Every other edge this move
# touches was already covered by the existing `inventory -> {catalog,identity,models,services,
# sites,dtos,utils}` lines above (StockMovementService already depended on `services.
# {EventOutboxService,SupabaseBroadcastService}`, `sites.application.LocationService`, etc. before
# the move; only the *source* module of those dependencies changed, from `services` to
# `inventory`, which these lines already approve). No new `services -> inventory` edge either:
# EventOutboxService/KujiBoxService/ShipmentService (still in `services`) already had that edge
# approved from calling the pre-move `services.StockMovementService`; they call the same class
# under its new package now. As part of T-4's R-8 consolidation (.specs/phase-6-inventory/log.md),
# `LocationService.DEFAULT_SITE_CODE`/`getDefaultSiteId()` went public and both moved services now
# call it instead of duplicating the lookup - this removed their `SiteRepository` dependency
# entirely, so no `inventory -> sites.infrastructure`-shaped concern arises from that change either
# (the edge check is module-level, not package-level, and `inventory -> sites` was already
# approved).
#
# Sixth reviewed addition (2026-09-10, .specs/phase-6-inventory/log.md T-6): moved
# InventoryAggregateController/StockMovementController and their DTOs/mappers into inventory.api,
# and LocationAggregateController/Service/Repository (+ LocationWithCountsDTO) into
# sites.api/sites.application/sites.infrastructure per R-1. Four new edges, all mechanical (same
# underlying dependency as before the move, only the source module's name changed):
#   `inventory -> validation`  (TransferInventoryRequestDTO's @ValidTransferDestination annotation
#                                 -- was `dtos -> validation`, already approved, before the move)
#   `validation -> inventory`  (TransferDestinationValidator's ConstraintValidator<..,
#                                 TransferInventoryRequestDTO> -- was `validation -> dtos`, already
#                                 approved, before the move)
#   `sites -> models`          (LocationAggregateController/Service use models.enums.LocationType --
#                                 was `controllers -> models`/`services -> models`, both already
#                                 approved, before the move)
#   `sites -> utils`           (LocationAggregateRepository uses utils.TimestampUtils -- was
#                                 `repositories -> utils`, already approved, before the move)
# `sites -> models` combined with the pre-existing `models -> sites` edge (AuditLog-era R-2
# precedent) is a third bidirectional pair in this file, alongside `models <-> inventory` and
# `inventory <-> dtos` above -- same already-accepted legacy-technical-package tangle character,
# not a new business-module cycle: `models` here is exactly the legacy `models.enums` package
# (LocationType), not a real aggregate relationship, and `sites` gains no new edge into any real
# business module from this move.
# InventoryAggregateController's `getInventoryTotals` also stopped reaching
# InventoryTotalsRepository directly (now goes through InventoryQueries), so no new
# `inventory -> inventory` edge (same-module, not tracked) and the pre-existing
# `controllers -> inventory` edge for this specific call site is retired (a controller reaching a
# repository directly used to be one of ArchitectureTest's frozen
# repositoriesAreOnlyAccessedByServicesOrRepositories violations; regenerating that store shrank
# it by exactly one entry for this reason).
# `LocationInventoryController`/`LocationInventoryMapper`/`LocationInventoryResponseDTO`/
# `InventoryRequestDTO` were deliberately NOT moved in T-6 (R-9, new): LocationInventoryResponseDTO
# .item is catalog.api.ProductSummaryDTO, and LocationInventoryMapper `uses` catalog.api.ProductMapper
# -- moving them into inventory.api would create an inventory.api -> catalog.api edge, which
# modulesDoNotDependOnAnotherModulesApi forbids unconditionally (no baseline/exemption mechanism,
# unlike the frozen-store or module-edge rules). Resolving it properly needs a catalog-owned
# application-layer product-summary read contract inventory.api can consume instead of catalog's
# own transport DTO -- real design work, not a mechanical move. Deferred; see log.md T-6 entry.
#
# This replaces the previous FreezingArchRule-based `topLevelPackagesAreFreeOfCycles` check, which
# froze the full, multi-line cycle-path text (including specific example call-site edges, capped
# by ArchUnit's own truncation) for the pre-existing catalog/dtos/exceptions/identity/models/
# repositories/services/validation cyclic legacy component - dozens of such violations, each many
# lines long. That made hand-review and maintenance of the frozen text impractical: an unrelated
# new class anywhere in that already-cyclic component can change which concrete example edges get
# printed for a given cycle instance without changing which modules actually participate. (The
# underlying cycle computation itself is deterministic for a fixed set of imported classes -
# verified by independently diffing raw, sorted violation output across separate JVM runs; the
# problem was the practicality of keeping a reviewable frozen snapshot of that much text in sync,
# not nondeterminism in the computation.) A new edge cannot be added without at least one class
# actually referencing another module, so - as long as this file is pruned on every genuine
# removal, per the note above - rejecting every unapproved new edge also rejects every new
# cross-module cycle, while staying small enough to actually read during review. It does not by
# itself resolve the pre-existing tangle within this edge set - that is unchanged, already-accepted
# debt.
analytics -> catalog
analytics -> inventory
analytics -> models
analytics -> repositories
auth -> identity
auth -> shared
catalog -> identity
catalog -> services
catalog -> shared
catalog -> sites
config -> identity
config -> models
config -> repositories
controllers -> analytics
controllers -> catalog
controllers -> dtos
controllers -> identity
controllers -> inventory
controllers -> models
controllers -> repositories
controllers -> services
controllers -> sites
displays -> catalog
displays -> repositories
dtos -> catalog
dtos -> identity
dtos -> inventory
dtos -> models
dtos -> services
dtos -> sites
dtos -> validation
exceptions -> catalog
exceptions -> identity
exceptions -> inventory
exceptions -> sites
identity -> services
identity -> shared
identity -> sites
inventory -> catalog
inventory -> dtos
inventory -> identity
inventory -> models
inventory -> repositories
inventory -> services
inventory -> sites
inventory -> utils
inventory -> validation
jobs -> repositories
jobs -> services
kuji -> catalog
kuji -> models
kuji -> repositories
models -> catalog
models -> identity
models -> inventory
models -> sites
repositories -> dtos
repositories -> identity
repositories -> models
repositories -> utils
services -> catalog
services -> dtos
services -> exceptions
services -> identity
services -> inventory
services -> kafka
services -> models
services -> repositories
services -> shared
services -> sites
shipments -> catalog
shipments -> repositories
sites -> models
sites -> shared
sites -> utils
validation -> dtos
validation -> inventory
validation -> models
