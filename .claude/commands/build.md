---
description: Implement one MiraiSD SDD task with tests and an execution log.
---

Read `AGENTS.md`, `docs/sdd-workflow.md`, and the active
`.specs/<feature-id>/spec.md`. Confirm the requested task exists before
editing. Work on one task at a time.

Use the globally installed `tdd-guide` for a meaningful local test path.
Record implementation decisions, assumptions, changed behavior, and test
results in `log.md`. Do not skip contract regeneration, authorization checks,
or migration safety when the task touches those boundaries.
